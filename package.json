{
  "name": "deadlink-crawler",
  "version": "3.0.1",
  "description": "Hybrid deadlink checker for JustFix front-end projects",
  "type": "module",
  "bin": {
    "deadlink-check": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && ncc build src/action.ts -o dist --external playwright --external @actions/core --external @actions/github --license licenses.txt && ncc build src/cli.ts -o dist-cli --external playwright && cp dist-cli/index.js dist/cli.js",
    "check": "node dist/cli.js",
    "typecheck": "tsc --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JustFixNYC/deadlink-crawler.git"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "glob": "^11.0.0",
    "playwright": "^1.49.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.2"
  }
}
